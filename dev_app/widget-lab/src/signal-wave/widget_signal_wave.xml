<?xml version="1.0" encoding="utf-8"?>
<!-- Signal Wave Widget Layout
     Design: Ultra-minimal with focus on the wave visualization
     Following Jony Ive's principle: "Simplicity is the ultimate sophistication"
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_signal_wave_bg"
    android:padding="8dp">

    <!-- Wave Canvas - The hero element -->
    <ImageView
        android:id="@+id/wave_canvas"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_gravity="center"
        android:scaleType="fitCenter"
        android:contentDescription="@string/wave_visualization" />

    <!-- Top Section - Ratio Display -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Main Ratio - Ultra-thin typography -->
        <TextView
            android:id="@+id/ratio_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="87"
            android:textSize="32sp"
            android:textColor="#FFFFFF"
            android:fontFamily="sans-serif-thin"
            android:includeFontPadding="false"
            android:layout_marginEnd="4dp" />

        <!-- Percent Symbol - Smaller, subtle -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="%"
            android:textSize="18sp"
            android:textColor="#AAAAAA"
            android:fontFamily="sans-serif-thin"
            android:includeFontPadding="false"
            android:layout_marginTop="8dp" />

        <!-- Spacer -->
        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <!-- Streak Container -->
        <LinearLayout
            android:id="@+id/streak_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="end"
            android:visibility="gone">

            <TextView
                android:id="@+id/streak_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="7 days"
                android:textSize="10sp"
                android:textColor="#00ff88"
                android:fontFamily="sans-serif-light" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="streak"
                android:textSize="9sp"
                android:textColor="#888888"
                android:fontFamily="sans-serif-light" />
        </LinearLayout>
    </LinearLayout>

    <!-- Bottom Section - Status and Time -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Status Text -->
        <TextView
            android:id="@+id/status_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Excellent Focus"
            android:textSize="11sp"
            android:textColor="#00ff88"
            android:fontFamily="sans-serif"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- Update Time - For transparency -->
        <TextView
            android:id="@+id/update_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="12:34"
            android:textSize="9sp"
            android:textColor="#666666"
            android:fontFamily="sans-serif-light" />
    </LinearLayout>

    <!-- AI Container - Premium Feature -->
    <LinearLayout
        android:id="@+id/ai_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="horizontal"
        android:gravity="center"
        android:visibility="gone"
        android:layout_marginTop="65dp">

        <!-- AI Pulse Indicator -->
        <View
            android:id="@+id/ai_pulse"
            android:layout_width="4dp"
            android:layout_height="4dp"
            android:background="@drawable/ai_pulse_dot"
            android:layout_marginEnd="6dp" />

        <!-- AI Insight Text -->
        <TextView
            android:id="@+id/ai_insight"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Keep momentum"
            android:textSize="10sp"
            android:textColor="#00ff88"
            android:fontFamily="sans-serif-light"
            android:maxLines="1"
            android:ellipsize="end"
            android:alpha="0.9" />
    </LinearLayout>

</FrameLayout>